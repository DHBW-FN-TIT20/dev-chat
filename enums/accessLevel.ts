/**
 * This Enum stores all existing AccessLevels
 * @category Enum
 */
export enum AccessLevel {
  USER,
  ADMIN,
  MAX_LEVEL,
}

/**
 * This Enum is used to pass pro-/demote option
 * @category Enum
 */
export enum ProDemote {
  PROMOTE,
  DEMOTE,
}