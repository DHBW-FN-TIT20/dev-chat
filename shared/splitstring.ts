/**
 * This helper function adds spaces to a String until its length is 16 Chars.
 * @param {string} str The string to append.
 * @returns The 16 char long string.
 */
 export default function splitString(str: string): number {
    //TODO
    return 0;
}
